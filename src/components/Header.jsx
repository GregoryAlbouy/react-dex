import './Header.scss';
import React from 'react';

const Header = () => (
    <header className="header">
        <h1>React-dex</h1>
        <h2>React + PokeAPI</h2>
    </header>
);

export default Header;